import { WorkspaceApp } from "@/components/workspace/WorkspaceApp";

export const metadata = {
  title: "Student Workspace — APSYLab",
  description: "Organize your psychology and computer science work in one place.",
};

export default function WorkspacePage() {
  return <WorkspaceApp />;
}
